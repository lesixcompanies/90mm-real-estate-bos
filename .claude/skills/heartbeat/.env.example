# Optional Slack webhook for the daily heartbeat ping.
# If unset, the SessionStart hook + inbox/desk-queue.md is the surface (works
# without this). Point it at a personal channel — it can fire once a day.
SLACK_WEBHOOK_URL=
